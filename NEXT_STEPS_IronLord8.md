# NEXT_STEPS_IronLord8.md

1. Create `.env` files from `.env.example` for local testing
2. Deploy backend to Render with correct `MONGO_URI` + `ODDS_API_KEY`
3. Deploy frontend to Vercel using VITE_ prefixed env vars
4. Test error handling by simulating broken components
5. Proceed to IronLord9: UI perfection, Steam/Alert tooling, mobile polish